[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npx prisma generate"]

[start]
cmd = "npx nest build && npx prisma migrate deploy && npm run start:prod"
