generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

// ==========================================
// TODO Prisma 2.1: Create a `User` model
// ==========================================
// 1. Fields: id (Int), email (String), name (String), createdAt, updatedAt
// 2. Map the table name to `users`

// ==========================================
// TODO Prisma 2.2: Create a `Profile` model
// ==========================================
// 1. Fields: id (Int), bio (String?), avatarUrl (String?), phone (String?)
// 2. Map the table name to `profiles`

// ==========================================
// TODO Prisma 2.3: Implement One-to-One relationship
// ==========================================
// 1. Link `Profile` to `User` inside the Profile model.
// 2. Add `@unique` on the `userId` foreign key to strictly enforce One-to-One!
